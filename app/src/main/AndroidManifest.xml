<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.netforceinfotech.tagalong">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.Base"
        tools:replace="android:theme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login1.LoginActivity" />
        <activity android:name=".login1.SignUpActivity" />
        <activity android:name=".login1.ForgotPasswordActivity" />
        <activity android:name=".home1.HomeActivity" />
        <activity android:name=".driverProfile1.DriverProfile" />
        <activity android:name=".myCars1.MyCarActivity" />
        <activity android:name=".myCars1.RegisterNewCar" />
        <activity android:name=".home1.findride.CantFindRideActivity"></activity>
        <activity android:name=".home1.offerride.RideDetailActivity" />
        <activity android:name=".home1.offerride.CarDetailActivity" />
        <activity android:name=".myprofile1.MyProfileActivity" />
        <activity android:name=".chat.MyChatActivity"></activity>
    </application>

</manifest>